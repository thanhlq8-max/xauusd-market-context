{
  "title": "session",
  "type": "object",
  "additionalProperties": true,
  "required": ["ts_utc", "symbol", "monitor_only", "session_state", "primary_timeframe", "session_ranges", "confidence_cap", "quality_flags"],
  "properties": {
    "monitor_only": {"type": "boolean", "const": true}
  }
}
