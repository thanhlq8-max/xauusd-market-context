# Case Library CLI Artifacts

The offline CLI writes exactly two curated seed artifacts under the requested `--out-dir`:

```text
case_library_seed.json
case_library_seed.md
```

These files are research/validation artifacts. They are not trading signals and are not part of the current market-context runtime artifact contract.
