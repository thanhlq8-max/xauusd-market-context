# Case Library CLI README

Use this README as the short operator entry point for the offline case-library command.

```bash
python -m xau_lfx.validation.case_library_cli \
  --event-log data/events/event_replay_template.csv \
  --out-dir case-library-seed
```

Artifacts:

- `case_library_seed.json`
- `case_library_seed.md`

Status:

```text
offline
monitor-only
research/validation artifact boundary
```
